# Никаких преобразований переводов строк. Не перестраховка: батники в проекте
# живут в CP866 и требуют CRLF — git, «причесав» их под LF, молча ломает разбор
# команд в cmd, а правки по `\n` перестают находить строки.
* -text
